import os
import time
from typing import List, Tuple
import itertools


def play(player_one_start: int, player_two_start: int) -> int:

    return None


if __name__ == "__main__":
    start_time = time.time()
    play()
    print("Process finished --- %s seconds ---" % (time.time() - start_time))
